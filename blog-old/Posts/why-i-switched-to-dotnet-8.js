// posts/why-i-switched-to-dotnet-8.js
// Auto-generated by composer.html — do not edit manually
// To update: re-export from composer and replace this file.

window.POST_BODY = `
  <p>When .NET 8 dropped, I'll be honest — I wasn't rushing to migrate. We had stable .NET 6 services running in production, a team that knew the codebase, and no burning reason to rock the boat. Then I ran the benchmarks.</p>

  <h2>The Performance Numbers</h2>
  <p>Our most-hit API endpoint — a product search aggregating from three internal services — dropped from an average of <strong>142ms to 89ms</strong> response time. That's a 37% improvement without touching a single line of business logic.</p>
  <p>The gains come from improvements across the runtime: better JIT compilation, faster GC, and Frozen Collections for read-only data.</p>

  <pre><code class="language-csharp">// Frozen collections — perfect for lookup tables loaded at startup
var statusMap = new Dictionary&lt;string, int&gt;
{
    ["active"]   = 1,
    ["inactive"] = 0,
    ["pending"]  = 2
}.ToFrozenDictionary();</code></pre>

  <h2>Primary Constructors</h2>
  <p>Services with constructor injection went from boilerplate-heavy to clean:</p>

  <pre><code class="language-csharp">// Before (.NET 6)
public class OrderService
{
    private readonly IOrderRepository _repo;
    private readonly ILogger&lt;OrderService&gt; _logger;

    public OrderService(IOrderRepository repo, ILogger&lt;OrderService&gt; logger)
    {
        _repo   = repo;
        _logger = logger;
    }
}

// After (.NET 8) — same result, half the lines
public class OrderService(IOrderRepository repo, ILogger&lt;OrderService&gt; logger)
{
    public async Task&lt;Order&gt; GetAsync(int id)
    {
        logger.LogInformation("Fetching order {Id}", id);
        return await repo.GetByIdAsync(id);
    }
}</code></pre>

  <h2>Native AOT for Lambdas</h2>
  <p>For Azure Functions and lightweight microservices, Native AOT compilation cut cold start times dramatically — services that took 800ms now come up in under 120ms.</p>

  <div class="callout callout-warn">
    <span class="callout-icon">⚠️</span>
    <span>AOT has trade-offs — reflection-heavy libraries need adaptation. Test thoroughly before enabling it on complex services.</span>
  </div>

  <h2>Should You Migrate?</h2>
  <p>If you're on .NET 6 (LTS), support runs until May 2027. But the performance wins alone justify moving. Start with a single service, measure before and after, and let the numbers make the case to your team.</p>

  <div class="callout callout-tip">
    <span class="callout-icon">💡</span>
    <span>Use the <code>dotnet-upgrade-assistant</code> CLI tool — it generates a migration checklist tailored to your project and catches breaking changes early.</span>
  </div>

  <hr>
  <p>The migration took about two sprints per service. The performance and developer experience payoff has been well worth it on every project since.</p>
`;
